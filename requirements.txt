# bilibili-intro-generator - runtime dependencies
# Install: pip install -r requirements.txt
# bilibili-api-python is fine from PyPI (pip install bilibili-api-python).

# ---- data fetching ----
bilibili-api-python>=16.0.0      # Bilibili API (video / opus / comments / danmaku)
aiohttp>=3.8.0                   # async HTTP client (danmaku / comments / fans / download)
aiofiles>=22.0.0                 # async file writing (video download)

# ---- config & parsing ----
PyYAML>=6.0                      # YAML config parsing

# ---- screenshot rendering ----
playwright>=1.30.0               # headless browser screenshot (intro image)
# Note: the program auto-detects a system browser (Edge -> Chrome -> Firefox -> Safari),
#       so no extra download is needed. Only if NO browser is available, run:
#       playwright install chromium
